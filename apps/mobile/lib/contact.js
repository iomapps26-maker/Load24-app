// Single source of truth for the sales team's phone number, shared by every
// "Call Sales Team" entry point (home screen, support tickets) so they can't
// drift out of sync with each other.
export const SALES_PHONE = '9555921555';
